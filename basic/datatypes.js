"use strict"; //it treats all JS version as new version

// alert("Hello World") //it popups hello world message in browser but it is only applicable in browser note in nodejs

/*
    TYPES OF DATATYPE

    int
    bigint
    string = " " or ' '
    boolean = true or false
    null = standalone value
    undefined = value not defined 
    symbol = unique 

*/

// Objects
console.log(typeof timarp);